{% extends "frontend/base.html.twig" %}

{% set item = knp_menu_get_current_item('frontend_main') %}

{% block title %}{{ menu }} · {{ parent() }}{% endblock %}

{% block submenu_header %}
    {{ knp_menu_render(item, {'currentClass': 'active', 'depth': 1}) }}
{% endblock %}

{% block main %}
    <div class="row">
        <div class="col">
            <h1>{{ menu }}</h1>
            <pre>#{{ menu.id }}</pre>
{#            <pre>{{ dump(knp_menu_get_breadcrumbs_array(item)) }}</pre>#}
{#            <pre>{{ menu.name }}</pre>#}

{#            <ul class="nav">#}
{#                {% for item in menu.menuLevel2items %}#}
{#                    <li class="nav-item">#}
{#                        <a class="nav-link active" aria-current="page" href="{{ path('front_app_menu_level_2', {menu: menu.slug, submenu: item.slug}) }}">{{ item.name }}</a>#}
{#                    </li>#}
{#                {% endfor %}#}
{#            </ul>#}
        </div>
    </div>
{% endblock %}
