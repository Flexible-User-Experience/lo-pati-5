{% extends "frontend/base.html.twig" %}

{% set item = knp_menu_get_current_item('frontend_main') %}

{% block title %}{{ submenu }} · {{ menu }} · {{ parent() }}{% endblock %}

{% block submenu_header %}
    <header class="d-flex border-bottom">
        <nav class="submenu navbar navbar-light bg-white text-uppercase mx-auto">
            {{ knp_menu_render(item.parent, {'currentClass': 'active', 'depth': 1}) }}
        </nav>
    </header>
{% endblock %}

{% block main %}
    <div class="row my-5">
        <div class="col">
            <h1>{{ submenu }} <span class="text-black-50">· {{ menu }}</span></h1>
            {% if submenu.page %}
                {% if app.environment == constant('App\\Kernel::DEV_ENV') %}
                    <hr>
                    <pre>P_ID# {{ submenu.page.id }} · PTT# '{{ submenu.page.templateTypeTransString | trans }}'</pre>
                {% endif %}
            {% else %}
                <hr>
                <pre>TO DO... {# TODO render empty menu level 2 template #}</pre>
            {% endif %}
        </div>
    </div>
    {% if submenu.page %}
        {% include 'frontend/partials/page_template_types/'~submenu.page.templateTypeString~'.html.twig' %}
    {% endif %}
{#    <div class="row">#}
{#        <div class="col">#}
{#            <h4>Pages list</h4>#}
{#            <ul class="nav flex-column">#}
{#                {% for page in submenu.pages %}#}
{#                    <li class="nav-item">#}
{#                        <a class="nav-link active" href="{{ path('front_app_page_detail', {'menu': menu.slug, 'submenu': submenu.slug, 'date': page.publishDate | date(constant('App\\Entity\\AbstractBase::DATAGRID_TYPE_DATE_FORMAT')), 'page': page.slug }) }}">{{ page.name }}</a>#}
{#                    </li>#}
{#                {% endfor %}#}
{#            </ul>#}
{#        </div>#}
{#    </div>#}
{% endblock %}
