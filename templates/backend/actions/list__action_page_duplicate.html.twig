{% if admin.hasAccess('edit', object) and admin.hasRoute('duplicate') %}
    {% set button_content = sonata_config.getOption('list_action_button_content') %}
    <a
            href="{{ admin.generateObjectUrl('duplicate', object, actions.link_parameters | default([])) }}"
            class="btn btn-sm btn-default edit_link"
            title="{{ 'back.action.duplicate' | trans({}, 'messages') }}"
    >
        {% if button_content == 'icon' or button_content == 'all' %}
            <i class="far fa-copy" aria-hidden="true"></i>
        {% endif %}
        {% if button_content == 'text' or button_content == 'all' %}
            {{ 'back.action.duplicate' | trans({}, 'messages') }}
        {% else %}
            <span class="sr-only">
                {{ 'back.action.duplicate' | trans({}, 'messages') }}
            </span>
        {% endif %}
    </a>
{% endif %}
